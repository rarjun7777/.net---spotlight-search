<Window x:Class="SearchApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="AI Search" 
        Height="180" 
        Width="500"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        WindowStartupLocation="CenterScreen">
    <Grid MouseLeftButtonDown="Grid_MouseLeftButtonDown">
        <Border Width="440" Height="80" Background="White" CornerRadius="12" VerticalAlignment="Center" HorizontalAlignment="Center">
            <Border.Effect>
                <DropShadowEffect Color="Black" BlurRadius="18" ShadowDepth="0" Opacity="0.18"/>
            </Border.Effect>
            <Grid>
                <TextBox x:Name="SearchTextBox"
                         FontSize="20"
                         FontFamily="Segoe UI"
                         Margin="20"
                         BorderThickness="0"
                         Background="Transparent"
                         VerticalContentAlignment="Center"
                         KeyDown="SearchTextBox_KeyDown"
                         />
            </Grid>
        </Border>
    </Grid>
</Window> 